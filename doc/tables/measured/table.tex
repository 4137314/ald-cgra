\begin{table}[t]
  \centering
  \caption{Current v2 versus current v3. Transactions and total TX+RX bytes;
  ID excluded, one call per workload. These are counters, not timings.}
  \label{tab:measured}
  \small
  \input{build/wire-table}
\end{table}
