\begin{figure}[t]
  \centering
  \resizebox{\columnwidth}{!}{\input{figures/pe/diagram}}
  \caption{Processing-element datapath: two operand muxes select from the four
  neighbours, the immediate, the self register or zero. MAC/ACC also use the
  direct accumulator feedback. The register captures on step; reset clears it.}
  \label{fig:pe}
\end{figure}
