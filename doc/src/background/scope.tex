\subsection{Scope of the project}

The contribution is a reproducible teaching system connecting configurable
RTL, a serial protocol, a C API and a declarative front end. Functional
simulation, independent scalar kernel references and measured host byte
counts provide different forms of evidence. Neither emulator execution nor
RTL simulation constitutes a measurement on an FPGA board.
